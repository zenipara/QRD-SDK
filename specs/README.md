# Deprecated Specs Directory

The `specs/` directory contains legacy specification documents that have been replaced by the consolidated documentation under `docs/FORMAT_SPEC.md`, `docs/architecture/ARCHITECTURE.md`, and the rest of the new documentation system.

Please use the `docs/` directory as the authoritative source.
