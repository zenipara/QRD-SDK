[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "qrd-sdk"
version = "0.1.0"
description = "QRD columnar binary format SDK for Python"
authors = [{name = "QRD Contributors"}]
license = {text = "MIT"}

[tool.maturin]
features = ["pyo3/extension-module"]
